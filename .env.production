# Clerk Configuration for Production Build
# This tells Clerk to skip edge middleware during static generation
CLERK_SKIP_MIDDLEWARE_DURING_BUILD=true

# Disable Clerk telemetry to reduce Edge Runtime bundle size
CLERK_TELEMETRY_DISABLED=1

# Force Clerk to use compatible crypto implementation
CLERK_EDGE_RUNTIME_COMPATIBLE=true
